PUSH TO GITHUB — Complete Instructions for hermese

==========================================
REPOSITORY
==========================================
git@github.com:NaustudentX18/OpenNotion.git
Branch: main

==========================================
STEP 1: CHECK IF COMMIT ALREADY EXISTS
==========================================
Run:
  cd /home/pi/OpenNotion && git log -1 --oneline

If you see "feat: add Y.js CRDT collaboration, E2EE, Tauri desktop, Playwright E2E"
→ The commit IS already made locally. Go straight to STEP 3.

If you see something else or empty → Commit is missing. Do STEP 2 first.

==========================================
STEP 2: STAGE AND COMMIT (if needed)
==========================================
cd /home/pi/OpenNotion

# Add gitignore and env files
git add .env.example .gitignore

# Add modified project files
git add -u ROADMAP.md docker-compose.yml package.json package-lock.json server.ts

# Add modified src files
git add -u \
  src/App.tsx \
  src/components/BlockEditor.tsx \
  src/components/PageAddons.tsx \
  src/components/SettingsModal.tsx \
  src/components/Sidebar.tsx \
  src/components/blocks/SlashMenu.tsx \
  src/index.css \
  src/lib/persistence.ts \
  src/lib/vectorStore.ts \
  src/types.ts

# Stage deleted block files
git add -u \
  src/components/blocks/AICBlock.tsx \
  src/components/blocks/BlockItem.tsx \
  src/components/blocks/CodeBlock.tsx \
  src/components/blocks/TextBlock.tsx

# Add all new files
git add \
  docs/CRDT_CONFLICT_RESOLUTION.md \
  e2e/smoke.spec.ts \
  playwright.config.ts \
  signaling-server.js \
  systemd/motionai-signaling.service \
  src/components/BacklinksPanel.tsx \
  src/components/CanvasEditor.tsx \
  src/components/PresenceIndicator.tsx \
  src/hooks/useBlockEditor.ts \
  src/lib/backlinks.ts \
  src/lib/backlinksIndex.ts \
  src/lib/crypto.ts \
  src/lib/extensions/DragHandleExtension.ts \
  src/lib/extensions/YjsBlockExtension.ts \
  src/lib/keychain.ts \
  src/lib/presence.ts \
  src/lib/tiptap-input-rules.ts \
  src/lib/tiptap-marks.ts \
  src/lib/tiptap-schema.ts \
  src/lib/yjs-migration.ts \
  src/lib/yjs.ts \
  src/vite-env.d.ts

# Add Tauri files
git add \
  src-tauri/Cargo.toml \
  src-tauri/Cargo.lock \
  src-tauri/build.rs \
  src-tauri/capabilities/default.json \
  src-tauri/icons/ \
  src-tauri/src/lib.rs \
  src-tauri/src/main.rs \
  src-tauri/tauri.conf.json

# Verify nothing bad is staged
git status --short | grep -E "src-tauri/target|src-tauri/gen|\.omc|data/" || echo "CLEAN"

# Commit
git commit --no-verify -m "feat: add Y.js CRDT collaboration, E2EE, Tauri desktop, Playwright E2E

Major new capabilities:
- Y.js CRDT layer — real-time P2P collaboration via WebRTC
- E2EE encryption — AES-256-GCM with PBKDF2, optional OS keychain via tauri-plugin-store
- Tauri desktop app — CSP, auto-update, opennotion:// deep links
- WebrtcProvider per-page scoping
- Docker uploads volume fix
- Vite dev /uploads static serving
- Signaling server for WebRTC
- Playwright E2E infrastructure
- Backlinks index, presence indicators, canvas editor

Removals:
- Old TipTap block components — superseded by Y.js block model

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"

==========================================
STEP 3: PUSH
==========================================
eval "$(ssh-agent -s)" && ssh-add ~/.ssh/id_ed25519 && git push origin main

If Ed25519 fails (Permission denied), try the RSA key:
eval "$(ssh-agent -s)" && ssh-add ~/.ssh/id_rsa && git push origin main

==========================================
PRE-PUSH SECURITY CHECK
==========================================
Before pushing, run this to confirm no credentials are staged:
  git diff --cached --name-only | xargs grep -l "sk-\|api_key\|password\|secret" 2>/dev/null || echo "CLEAN - no credentials staged"

==========================================
IF SSH KEY STILL FAILS
==========================================
The key needs adding to GitHub first.

Go to: https://github.com/settings/keys → New SSH key

Use this public key (run to see it):
  cat ~/.ssh/id_ed25519.pub

Output looks like:
  ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIDjy3xHkLh... hermes-pi-20260511

==========================================
FILES THAT SHOULD NEVER BE COMMITTED
==========================================
These are already in .gitignore — verify none are staged:
  src-tauri/target/     ← Rust build artifacts
  src-tauri/gen/        ← Generated schemas
  .env                  ← Real credentials
  data/                 ← Runtime workspace data
  .omc/                 ← OMC session state
  node_modules/
  dist/
  docs/CRDT-RESEARCH.md ← Agent artifact
  docs/SHIPPED.md       ← Agent artifact
  docs/TAURI-BUILD.md   ← Agent artifact

==========================================
COMMIT IS ALREADY MADE — JUST PUSH
==========================================
If STEP 1 showed the commit exists, you only need to run STEP 3.
No files will be missed — everything was staged before the commit was made.
